# AADisplay-101 version configuration
# Edit this file and push to main to trigger a CI release build.
#
# VERSION_NAME  : SemVer display string (e.g. 1.0.0, 2.1.0-beta)
# VERSION_CODE  : Monotonically increasing integer for Android
# PRERELEASE    : true = GitHub release marked as pre-release; false = stable

VERSION_NAME=0.0.1-beta
VERSION_CODE=1
PRERELEASE=true
